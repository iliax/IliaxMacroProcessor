m1 MACRO x y 
.beg:
	.WHILE [$x > 0]
		print x $x
		.DEC x
		.SET y 2
		.WHILE [$y > 0]
		  print ololo
		  .DEC y
		.IF [$x == 8]
		  .SET x 0
		  .GOTO end
		.ENDIF
		.ENDW
	.ENDW

.end: 
	
MEND

m2 MACRO x, y=1

 print $x
 print $y

MEND

m2 10, y=111
















###################
add
del
print
